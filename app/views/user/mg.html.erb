    <!-- t.text "content"
    t.datetime "created_at", null: false
    t.datetime "updated_at", null: false
    t.integer "user_id"

  create_table "users", force: :cascade do |t|
    t.string "name"
    t.string "email"
    t.string "password"
    t.datetime "created_at", null: false
    t.datetime "updated_at", null: false
    t.string "img_name"
     -->
  <h1>ユーザー一覧</h1>
  <p>ここはユーザー一覧です(管理画面想定)</p>

  <div class="user-index">
    <h2>user</h2>
    <table>
      <tr><th>id</th><th>admin</th><th>user-name</th><th>email</th><th>password</th><th>img_name</th></tr>
      <% @users.each do |user| %>
      <tr>
        <td>
          <%= user.id %>
        </td>
        <td>
          <%= user.admin %>
        </td>
        <td>
          <%= link_to(user.name, "/user/#{user.id}") %>
        </td>
        <td>
          <%= user.email %>
        </td>
        <td>
          <%= user.password_digest %>
        </td>
        <td>
          <%= user.img_name %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="user-index">
    <h2>post</h2>
    <table>
      <tr><th>id</th><th>content</th><th>user_id</th><th>created_at</th><th>updated_at</th></tr>
      <% @posts.each do |post| %>
      <tr>
        <td>
          <%= post.id %>
        </td>
        <td>
          <%= link_to(post.content, post_path(post.id)) %>
        </td>
        <td>
          <%= post.user_id %>
        </td>
        <td>
          <%= post.created_at %>
        </td>
        <td>
          <%= post.updated_at %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="user-index">
    <h2>like</h2>
    <table>
      <tr><th>id</th><th>post_id</th><th>user_id</th></tr>
      <% @likes.each do |like| %>
      <tr>
        <td>
          <%= like.id %>
        </td>
        <td>
          <%= like.post_id %>
        </td>
        <td>
          <%= like.user_id %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
